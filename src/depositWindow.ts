/**
 * Deposit PFV — one-click SPL transfer to treasury with memo.
 * Placeholder: full flow requires deposit-intent API and on-chain verification.
 */
export function openDepositWindow(): void {
  alert("Deposit PFV — coming soon. Configure PFV mint and treasury on the server.");
}
