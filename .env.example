# API base URL. Leave empty for same-origin.
VITE_API_URL=https://pixelvalley.farm

# Reown AppKit project ID — required for wallet connect (Phantom, Solflare, etc.)
# Get one at https://cloud.reown.com
VITE_WALLETCONNECT_PROJECT_ID=your_project_id_here

# When "true", LAUNCH GAME redirects to pixelvalley.farm (use for landing-only deploy, e.g. GitHub Pages)
VITE_REDIRECT_LAUNCH_TO_LIVE=false
