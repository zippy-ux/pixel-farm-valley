# Contributing

## Do not commit secrets

- Never commit `.env` files or real API keys
- Use `.env.example` as a template; add your own values in a local `.env` (gitignored)
- Do not put WalletConnect project IDs, tokens, or private keys in the repository
